import { IncidentInterfaces } from './incident.interfaces';

describe('IncidentInterfaces', () => {
  it('should create an instance', () => {
    expect(new IncidentInterfaces()).toBeTruthy();
  });
});
